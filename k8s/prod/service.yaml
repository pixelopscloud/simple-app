apiVersion: v1
kind: Service
metadata:
  name: simple-app-service
  namespace: prod
  labels:
    app: simple-app
    environment: prod
spec:
  type: NodePort
  selector:
    app: simple-app
    environment: prod
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30002
    protocol: TCP
