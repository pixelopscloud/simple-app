apiVersion: v1
kind: Service
metadata:
  name: simple-app-service
  namespace: dev
  labels:
    app: simple-app
    environment: dev
spec:
  type: NodePort
  selector:
    app: simple-app
    environment: dev
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30001
    protocol: TCP
